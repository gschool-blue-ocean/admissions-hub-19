import React from "react";

// TODO - add a page name prop to change the page name in the header
const Header = () => {
  return (
    <div className="layout-header">
      <h3 className="">Dashboard</h3>
    </div>
  );
};

export default Header;
